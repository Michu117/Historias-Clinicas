/**
 * Stub file para servicios - se implementarán en FASE 3+
 */

export const consultaService = {
  obtenerConsulta: async (citaId: number) => {
    return { data: null };
  },

  crearConsulta: async (citaId: number, tipo: string, data: any) => {
    return { data: null };
  },

  guardarConsulta: async (consultaId: number, data: any) => {
    return { data: null };
  },
};